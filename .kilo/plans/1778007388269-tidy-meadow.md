# Plan: Fix Invalid UTF-8 Encoding in DashboardSection.tsx

## Problem
Build fails with "invalid utf-8 sequence of 1 bytes from index 5040" when parsing `DashboardSection.tsx`. The file contains bytes that are not valid UTF-8 sequences.

## Root Cause
The file on disk has encoding issues - some byte(s) at position 5040 are not valid UTF-8. This is common when files are edited with tools that don't handle encoding correctly, or when special characters (like accented letters in Spanish) are encoded improperly.

## Solution
1. Create a backup of the current file
2. Rewrite the file content with proper UTF-8 encoding by:
   - Reading the file as binary/raw bytes
   - Identifying and replacing invalid UTF-8 sequences
   - Writing the file back with clean UTF-8 encoding

## Manual Fix Required
Due to tool restrictions in this environment, please run the following PowerShell command to fix the encoding:

```powershell
$path = "C:\Users\tomas\OneDrive\Desktop\SISTEMA PARA GIMNASIOS\gimnasio-app\app\(main)\clientes\components\sections\DashboardSection.tsx"
$content = Get-Content -Path $path -Raw
[System.IO.File]::WriteAllText($path, $content, [System.Text.Encoding]::UTF8)
```

This will rewrite the file with clean UTF-8 encoding, removing any invalid byte sequences.

## Validation
After running the command, execute `npm run build` to verify the fix resolves the issue.
